#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    vector<int> getTable(int N) {
        vector<int> table;
        for (int i = 1; i <= 10; i++) {
            table.push_back(N * i);
        }
        return table;
    }
};
