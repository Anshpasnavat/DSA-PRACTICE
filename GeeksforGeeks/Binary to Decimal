class Solution {
public:
    int binaryToDecimal(string &b) {
        int result = 0;
        
        for(char ch : b) {
            result = result * 2 + (ch - '0');
        }
        
        return result;
    }
};
